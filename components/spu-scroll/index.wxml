<view class="container">
  <image src="{{theme.title_img}}" class="title" />
  <scroll-view>
    <block wx:for="{{spuList}}">
      <view>
        <image src="{{item.img}}" />
        <l-price autofix color="#157658" l-value-class="price-value" l-unit-class="price-unit" value="{{item.price}}" />
        <text>{{item.title}}</text>
      </view>
    </block>
  </scroll-view>
</view>